Per questa parte frontend di react native serve connettersi ad un terminale che esegua il file "server.py" nella cartella servers che farà da server (python server.py).

Prima di tutto ci assicuriamo di avere la versione di node aggiornata con il comando "nvm install node".

Per far funzionare testnative abbiamo bisogno di installare alcune dipendenze:

-npm install @react-navigation/native @react-navigation/stack axios
-npm install react-native-gesture-handler react-native-reanimated react-native-screens react-native-safe-area-context @react-native-community/masked-view

Per avviare testnative bisogna entrare nella cartella (cd testnative) da un nuovo terminale tenendo aperto quello del server e poi fare il comando "npm run web" per vedere la versione da pc.

