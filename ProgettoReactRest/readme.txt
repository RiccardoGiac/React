Per questa parte frontend di react serve connettersi ad un terminale che esegua il file "server1.py" nella cartella servers che farà da server (python server1.py).

Il server1 collegato ad un database con postgres, non ho avuto tempo di cambiarlo e sostituirlo con un json in quanto erano gia passate diverse ore per il test 
quando io venni a conoscenza di doverlo collegare in tal modo. Tuttavia il tutto funzionava.



Prima di tutto ci assicuriamo di avere la versione di node aggiornata con il comando "nvm install node".

Per far funzionare questo progetto abbiamo bisogno di installare alcune dipendenze:

-npm install axios
-npm install react-router-dom


Per avviare questo progetto bisogna aprire un nuovo terminale lasciando aperto quello del server ed entrare nella cartella (cd ProgettoReactRest) e poi fare il comando "npm run dev".

